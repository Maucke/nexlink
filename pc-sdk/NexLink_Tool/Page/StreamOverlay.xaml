<Window x:Class="NexLink_Tool.Page.StreamOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Width="280" Height="300"
        ResizeMode="CanResizeWithGrip">
    <Border BorderThickness="1" BorderBrush="#CCFF0000">
        <Grid>
            <Border Background="#CC000000" Height="24" VerticalAlignment="Top">
                <Grid>
                    <TextBlock Text="📺 [ESC=Stop]"
                               Foreground="White" FontSize="11"
                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                    <Button x:Name="CloseBtn" Content="✕"
                            Foreground="White" Background="Transparent"
                            BorderThickness="0" FontSize="12"
                            HorizontalAlignment="Right" Width="24" Padding="0" Cursor="Arrow"/>
                </Grid>
            </Border>
            <TextBlock x:Name="InfoText"
                       Foreground="#AAFFFFFF" FontSize="10"
                       VerticalAlignment="Bottom" HorizontalAlignment="Left"
                       Margin="6,0,0,3"/>
        </Grid>
    </Border>
</Window>
